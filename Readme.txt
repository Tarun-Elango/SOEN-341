2. Read the assembly source file (assume the file is validated and exists). 
(a) Open the source file in read mode.                --done
(b) Create and open a file input stream.              --done

3. Scan characters to extract and create tokens. 
(a) Create a symbol table (for labels and mnemonics). --done
(b) Enter mnemonics (keywords) in the symbol table.   --done
(c) Read characters from the input stream.            --done
